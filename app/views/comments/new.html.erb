<h1>Comment on <%=@activity.name%> </h1><br>

  <%= form_with model: @comment, local: true, :url => activity_comments_path do |f| %>
  <%= f.label :comment%>
  <%= f.text_area :comment%><br>

  <%= f.hidden_field :user_id, :value=> session[:user_id] %><br>
  <%= f.submit%>
  <%end%>
